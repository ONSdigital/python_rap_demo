# RAP user configuration file
#
# This file stores any parameters that anyone running the pipeline may want to change.
# Such as:
# - File paths for input, output, or intermediate data
# - Parameters for cleaning or processing (e.g., columns to drop, thresholds)
# - Report settings (e.g., title, author, format)
# - Analysis options (e.g., which features to include, which months to process)
# - Any other settings you want to change without editing code
cleaned_path: data/health_data_cleaned.csv
report_path: data/disease_prevalence_report.md
